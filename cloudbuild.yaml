steps:
  - name: 'gcr.io/cloud-builders/gsutil'
    args:
      - '-h'
      - 'Cache-Control:no-cache, max-age=0'
      - 'cp'
      - 'index.html'
      - 'gs://musica-frontend-test/index.html'

options:
  logging: CLOUD_LOGGING_ONLY

timeout: '120s'
